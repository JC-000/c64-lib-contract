# Library Adopters

This page tracks which libraries adopt (or are in the process of adopting) the [c64-lib-contract](SPEC.md). Each row shows the library's state per contract section; tracking issue links point at the library's own repo.

## Adoption status

✅ shipped &nbsp; ⚠️ partial &nbsp; ❌ todo &nbsp; n/a not applicable

| Library | §1 `LIB_VERSION_*` | §2 `.exportzp` ZP | §3 REU symbol contract | §4 Segment naming | §5 Manifest equates | §6 Build target variants | §8 Shared primitives | §8.0 Precalc tables enumerated | §1/§8.4 prefixed exports (v0.7.0) |
|---|---|---|---|---|---|---|---|---|---|
| [c64-nist-curves](https://github.com/JC-000/c64-nist-curves) | ✅ shipped (v0.2.0; `LIB_VERSION_*` + `LIB_ABI_VERSION` via [PR #48](https://github.com/JC-000/c64-nist-curves/pull/48)) | ✅ shipped (`src/zp_config.s`) | ✅ shipped ([PR #43](https://github.com/JC-000/c64-nist-curves/pull/43); `LIB_NISTCURVES_REU_BANK_*` in `src/reu_config.s`) | ✅ shipped ([#41](https://github.com/JC-000/c64-nist-curves/issues/41) closed; segments `LIB_NISTCURVES_P256/P384/SHA384/MUL_*`) | ✅ shipped ([#42](https://github.com/JC-000/c64-nist-curves/issues/42) closed; `src/lib_manifest.s`) | ✅ shipped ([#40](https://github.com/JC-000/c64-nist-curves/issues/40) closed; nine `make lib-*` targets emitting `build/lib/nistcurves[-<variant>].a` at SPEC §6 paths — five default-profile (`lib` / `lib-p256-verify` / `lib-p384-verify` / `lib-p384-sha384` / `lib-p384-curve`) plus four `FP_ONCHIP_MUL` turbo-profile variants shipped in v0.5.0 via [PR #70](https://github.com/JC-000/c64-nist-curves/pull/70) ([#69](https://github.com/JC-000/c64-nist-curves/issues/69)): `lib-onchip` / `lib-p256-verify-onchip` / `lib-p384-verify-onchip` / `lib-p384-curve-onchip` — no `-onchip` SHA-384 variant, that archive carries no field multiply; per-variant symbol inventory ratcheted by `make check-archives` against the library's `API.md` §8.4.1) | ✅ shipped (§8.1 `sqtab` via [PR #50](https://github.com/JC-000/c64-nist-curves/pull/50); §8.2 `reu_mul` via [PR #55](https://github.com/JC-000/c64-nist-curves/pull/55); §8.3 `ct_mul_8x8` canonical body via [PR #56](https://github.com/JC-000/c64-nist-curves/pull/56) + bit `$0004` via [PR #57](https://github.com/JC-000/c64-nist-curves/pull/57); `LIB_SHARED_SQTAB_BASE` in `src/mul_8x8.s`, `LIB_SHARED_REU_MUL_BANK` in `src/reu_config.s`; `LIB_NISTCURVES_SHARED_PRIMITIVES` in `src/lib_manifest.s` uses the §8.0 v0.4.0 conditional form — standalone `$0007`, each defined `SHARED_*` deferral switch drops its bit; per-profile: the `FP_ONCHIP_MUL` build's standalone mask is `$0005` — bit `$0002` is deliberately omitted rather than deferred through `SHARED_REU_MUL_INIT`, because that profile generates multiply rows on-chip and never builds or reads the §8.2 table, so there is no ownership claim to compose against; a permanent `.assert` keeps the bit clear under the profile ([#78](https://github.com/JC-000/c64-nist-curves/issues/78) via [PR #80](https://github.com/JC-000/c64-nist-curves/pull/80), mirroring the c64-x25519 sibling profile). The §8.2 provider `reu_mul_init` lives in its own `src/reu_mul_init.s` object, pulled into every default-profile REU-consuming archive through the Makefile's `LIB_MUL_OBJS` and deliberately excluded from the onchip ones, so each profile's `$0002` claim matches what its archives actually ship; `make check-archives` pins both directions ([#81](https://github.com/JC-000/c64-nist-curves/issues/81) via [PR #82](https://github.com/JC-000/c64-nist-curves/pull/82) — before that the provider was exported from `src/main.s`, a test-driver translation unit no archive includes, so default-profile archives declared the bit without shipping a linkable initializer) | ✅ shipped (§8.0 catch-loop via [PR #55](https://github.com/JC-000/c64-nist-curves/pull/55); [`docs/precalc-tables.md`](https://github.com/JC-000/c64-nist-curves/blob/master/docs/precalc-tables.md) + `src/precalc_manifest.s` `LIB_PRECALC_TABLE` invocations for `sqtab`, `reu_mul`, `lim_lee_comb_p256`, `lim_lee_comb_p384`, `sha384_k`; `precalc_table.inc` copied byte-identical from this repo at `b039ab9`; profile-conditional since [PR #80](https://github.com/JC-000/c64-nist-curves/pull/80) ([#78](https://github.com/JC-000/c64-nist-curves/issues/78)) — the `reu_mul` row is `.ifndef FP_ONCHIP_MUL`-gated, so the `precalc_manifest_onchip.o` the four onchip archives ship exports no `LIB_PRECALC_reu_mul_*` symbols, while the `lim_lee_comb_p256` / `lim_lee_comb_p384` rows stay unconditional (the onchip full / curve archives still populate and read REU bank `$02`; only the mul banks `$00`/`$01` disappear under the profile); `docs/precalc-tables.md` carries a per-row Profiles column) | ⚠️ partial — **on `main`, not tagged** ([#86](https://github.com/JC-000/c64-nist-curves/issues/86) closed via [PR #87](https://github.com/JC-000/c64-nist-curves/pull/87), merged 2026-08-13, bundling contract v0.5.0–v0.7.1). Newest tag `v0.8.0` predates the migration — verified: its `src/lib_version.s` has no `LIB_NISTCURVES_VERSION_*`. Verified at `master` after merge: `src/lib_version.s` exports `LIB_NISTCURVES_VERSION_{MAJOR,MINOR,PATCH}` / `LIB_NISTCURVES_ABI_VERSION` with the bare names aliased under `.ifndef LIB_NO_BARE_EXPORTS`, and exports nothing else (§1 TU isolation satisfied); all five `src/precalc_manifest.s` invocations pass `"NISTCURVES"`; `src/precalc_table.inc` is the v0.7.0 canonical. **First adopter fully conformant to v0.7.2 at `main`.** Reference implementation for the other three |
| [c64-polyval](https://github.com/JC-000/c64-polyval) | ✅ shipped (v0.3.0; `LIB_VERSION_*` + `LIB_ABI_VERSION` via closed [#12](https://github.com/JC-000/c64-polyval/issues/12); `src/lib_version.s`) | ✅ shipped (closed [#13](https://github.com/JC-000/c64-polyval/issues/13); `src/zp_config.s`, `polyval_*`/`pv_*` prefix) | ✅ n/a (no REU claims; `LIB_POLYVAL_REU_BANKS_USED = 0`) | ✅ shipped (closed [#14](https://github.com/JC-000/c64-polyval/issues/14); segments `LIB_POLYVAL_*_CODE/RODATA/BSS`) | ✅ shipped (closed [#15](https://github.com/JC-000/c64-polyval/issues/15); `src/lib_manifest.s`) | ✅ shipped (closed [#16](https://github.com/JC-000/c64-polyval/issues/16); targets `lib-polyval-long` / `lib-polyval-short` / `lib-polyval-gcmsiv`) | n/a (GF(2^128) carry-less mult; no shared shape with the 8×8 quarter-square-multiply libraries above — does not consume §8.1 `sqtab` / §8.2 `reu_mul` / §8.3 `ct_mul_8x8`) | ✅ shipped (v0.4.0 tag; `src/precalc_table.inc` copied verbatim + `LIB_PRECALC_TABLE` invocations in `src/lib_manifest.s` for `polyval_htable` (256 B, RAM, LONG+SHORT) / `polyval_htable8` + `polyval_reduce8` (4096 B each, RAM, LONG only) / `aes_sbox` + `aes_inv_sbox` (256 B each, RODATA); all `PRECALC_SHARED_NO`; rationale in [`docs/precalc-tables.md`](https://github.com/JC-000/c64-polyval/blob/v0.4.0/docs/precalc-tables.md); `precalc_table.inc`'s initial copy was transcribed from this SPEC's fenced §8.0 block rather than the canonical root file — comment-only divergence, fixed via [PR #18](https://github.com/JC-000/c64-polyval/pull/18)) | ❌ todo — [#21](https://github.com/JC-000/c64-polyval/issues/21) (§1 prefixed exports) + [#22](https://github.com/JC-000/c64-polyval/issues/22) (§8.4 fifth argument) filed 2026-08-13. Verified same day: `src/lib_version.s` exports bare `LIB_VERSION_*` / `LIB_ABI_VERSION` only; all five `LIB_PRECALC_TABLE` invocations in `src/lib_manifest.s` are the four-argument form. Prefix is `POLYVAL`. §1 TU isolation **already satisfied** — `src/lib_version.s` exports only those four symbols, §5 equates already live in `src/lib_manifest.s`, so this is additive with no file split. No live collision today (no shared table, no §8 consumption) |
| [c64-x25519](https://github.com/JC-000/c64-x25519) | ✅ shipped (v0.5.0, `src/lib_version.s`) | ✅ shipped (`src/zp_config.s`) | ✅ shipped (`src/reu_config.s` — `X25519_REU_BANK` / `X25519_REU_OFFSET`, ca65 `-D` override) | ✅ shipped (`LIB_X25519_INIT_CODE` reclaimable init segment via [c64-x25519#69](https://github.com/JC-000/c64-x25519/pull/69), `LIB_X25519_COLD_BYTES` real at 826/648 B; full `CODE`/`DATA` → `LIB_X25519_CODE`/`LIB_X25519_DATA` rename via [c64-x25519#71](https://github.com/JC-000/c64-x25519/pull/71) — pure rename, PRG byte-identical; harness sources keep plain segments per §4 library-sources scope) | ✅ shipped (`src/lib_version.s`) | ✅ shipped (v0.8.0 via [c64-x25519#73](https://github.com/JC-000/c64-x25519/pull/73); targets `lib` / `lib-x25519-1764` / `lib-x25519-onchip` → `build/lib/libx25519.a` / `libx25519-1764.a` / `libx25519-onchip.a` at §6 paths; 1764 variant omits the doubled-row REU tables (`SQR_DMA_K=0`, banks 0–1 only), onchip variant is the no-REU `X25519_ONCHIP_MUL` profile; `lib-x25519-scalarmult` minimal variant remains deferred — no current consumer requires it, [#45](https://github.com/JC-000/c64-x25519/issues/45)) | ✅ shipped (§8.1 `sqtab` via [PR #56](https://github.com/JC-000/c64-x25519/pull/56); §8.2 `reu_mul` via [PR #59](https://github.com/JC-000/c64-x25519/pull/59); §8.3 `ct_mul_8x8` canonical body via [PR #62](https://github.com/JC-000/c64-x25519/pull/62) + bit `$0004` via [PR #63](https://github.com/JC-000/c64-x25519/pull/63); `LIB_SHARED_SQTAB_BASE` default `$7800` in `src/constants.s`; `LIB_X25519_SHARED_PRIMITIVES` in `src/lib_version.s` uses the §8.0 v0.4.0 conditional form — standalone `$0007`, each defined `SHARED_*` deferral switch drops its bit; per-profile: the `X25519_ONCHIP_MUL` build's standalone mask is `$0005` — bit `$0002` deliberately omitted because that profile never initializes or reads the §8.2 REU table, so there is no claim to compose against) | ✅ shipped (§8.0 catch-loop via [PR #59](https://github.com/JC-000/c64-x25519/pull/59); `LIB_PRECALC_TABLE` invocations in `src/lib_version.s` for `sqtab` (1024 B, RAM, shared) / `reu_mul` (131072 B, REU, shared) / `reu_mul_doubled` (196608 B, REU, private); the `reu_mul` / `reu_mul_doubled` rows are build-conditional — `reu_mul` is `.if ::X25519_ONCHIP_MUL = 0`-gated (the onchip profile builds no §8.2 table), while `reu_mul_doubled` is gated on `SQR_DMA_K`, so it is absent from the 1764 variant (`SQR_DMA_K=0`) and from the onchip build (the profile forces `SQR_DMA_K=0`, `.assert`-enforced in `src/constants.s`) — matching the c64-ChaCha20-Poly1305 Profile A/B per-profile-export precedent; rationale in `docs/precalc-tables.md`; `precalc_table.inc` copied verbatim) | ⚠️ partial — **on `main`, not tagged.** [#78](https://github.com/JC-000/c64-x25519/issues/78) closed via [PR #82](https://github.com/JC-000/c64-x25519/pull/82), merged 2026-08-13 11:22Z. Verified at `main`: prefixed §1 exports with bare aliases under `.ifndef LIB_NO_BARE_EXPORTS`; the §1 TU split done — `src/lib_manifest.s` now exists and carries §5 + §8.4, so `src/lib_version.s` no longer drags bare names into a manifest-driven link; no `.export` of any `LIB_SHARED_PRIMITIVES_*` bit constant; all three invocations pass `"X25519"`; `LIB_X25519_SHARED_CONSUMES` exported. Newest tag `v0.8.0` predates the migration — verified: its `src/lib_version.s` has no `LIB_X25519_VERSION_*`. Was the worst TU-isolation case in the fleet; now resolved |
| [c64-ChaCha20-Poly1305](https://github.com/JC-000/c64-ChaCha20-Poly1305) | ✅ shipped (v0.6.0, `src/lib_version.s`); **v0.7.0 prefixed forms** via [PR #60](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/60) (closes [#53](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/53)) — `LIB_CHACHA20_POLY1305_VERSION_{MAJOR,MINOR,PATCH}` / `_ABI_VERSION`, bare four kept as aliases gated on `LIB_NO_BARE_EXPORTS`; §1 TU isolation was already satisfied | ✅ shipped (`src/zp_config.s`) | ✅ shipped via [#19](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/19) (`POLY1305_REU_BANK` / `POLY1305_REU_OFFSET`) | ✅ shipped via [PR #49](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/49) (closes [#48](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/48)); `CODE`/`DATA` → `LIB_CHACHA20_POLY1305_CODE`/`LIB_CHACHA20_POLY1305_DATA` across the five library sources, harness `main.s`/`zp_config.s` keep plain segments per §4 library-sources scope. Purity proved at object level rather than by PRG byte-identity: the 1-byte harness `lib_entry` stub holds `$0900`, so the page-aligned library segment starts at `$0A00` and the standalone PRG gains 255 B of pad no consumer pays — instead the library objects linked alone under equivalent pre/post cfgs are byte-identical across all four build configurations, per-object segment totals conserved exactly. Two cfg attributes are load-bearing and fail *silently*: `align = $100` on `LIB_CHACHA20_POLY1305_CODE` is a **CT invariant** (secret-indexed `.align 256` LUTs — ld65 only warns and links them misaligned if absent), and `LIB_CHACHA20_POLY1305_DATA` must stay `type = rw` in a file-emitting area or `sqtab_ready` reads power-on garbage | ✅ shipped (`src/lib/lib_manifest.s`) | ✅ shipped (v0.6.0 via [PR #35](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/35); targets `lib` / `lib-aead-only` → `build/lib/c64-chacha20-poly1305.a` / `c64-chacha20-poly1305-aead-only.a` at §6 paths; the aead-only variant strips test-only exports via `LIB_VARIANT_AEAD_ONLY`; default-off `POLY1305_MULTIPLY_ROLLED{,_OUTER}` size knobs compose with either archive — combined minimal consumer link 8,230 B, closing [#34](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/34)) | ✅ shipped (§8.1 `sqtab` via [PR #39](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/39); §8.3 `ct_mul_8x8` **canonical owner** — bit `$0004` + §8.0 v0.4.0 conditional mask via [PR #43](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/43); `LIB_SHARED_SQTAB_BASE` default `$8000` in `src/lib/poly1305_lib.s`; `LIB_CHACHA20_POLY1305_SHARED_PRIMITIVES` in `src/lib/lib_manifest.s` uses the conditional form — standalone `$0005`, each defined `SHARED_*` deferral switch drops its bit; ct_mul_8x8 SMC body uses equate-derived page math per §8.1 placement contract. **§8.3 deferral made real** via [PR #50](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/50) (closes [#47](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/47)): `SHARED_CT_MUL_8X8` previously flipped only the manifest bit while the body and `mul_8x8`/`poly_prod_lo`/`poly_prod_hi` exports stayed live, so a two-archive link against c64-x25519 v0.8.0 died on `Duplicate external identifier: 'poly_prod_hi'`; the switch now gates the body and imports the five §8.3 names from the owner, and `ct_mul_8x8` is **exported** so this library can finally serve as the §8.3 provider its canonical-owner claim advertises — verified functionally, a composed PRG with x25519 supplying `ct_mul_8x8` + `mul_tables_init` passes 214/214. **v0.5.0 three-state semantics** via [PR #56](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/56) (closes [#51](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/51), [#52](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/52)): masks are profile-gated, so Profile A reads `$0000`/`$0000` instead of over-claiming `$0005` for primitives #34 F1 removed from that profile, and `LIB_CHACHA20_POLY1305_SHARED_CONSUMES` is exported with the §8.0 subset assert. **§8.x bit constants un-exported** via [PR #60](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/60) (closes [#57](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/57) item 2) — `LIB_SHARED_PRIMITIVES_SQTAB` / `_CT_MUL_8X8` were `.export`ed, colliding with c64-x25519 on unprefixed identical-valued names; this survives the v0.7.0 prefixing and no SPEC clause asked for the export, matching the §13.0 `NET_FAMILY_*` rationale and c64-nist-curves' long-standing local-equate practice) | ✅ shipped (§8.0 catch-loop via [PR #42](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/42); `LIB_PRECALC_TABLE` invocations for `sqtab` / `chacha_nibswap_hi_tab` / `chacha_nibswap_lo_tab` / `r_tab_lo` / `r_tab_hi` in `src/lib/lib_manifest.s` (Profile B: 9 exports; **Profile A: 12** as of [PR #56](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/56) — the `sqtab` row is now profile-gated to match the consumes mask, since Profile A emits no sqtab); **v0.7.0 fifth macro argument** via [PR #60](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/60) (closes [#54](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/54)): `precalc_table.inc` re-copied verbatim from this repo's canonical, all five invocations pass `"CHACHA20_POLY1305"`, emitting `LIB_CHACHA20_POLY1305_PRECALC_*` beside the gated bare triple — the cross-library `.assert LIB_X25519_PRECALC_sqtab_SIZE = LIB_CHACHA20_POLY1305_PRECALC_sqtab_SIZE` passes against c64-x25519 v0.8.0; rationale + below-floor exempt list in `docs/precalc-tables.md`) | ⚠️ partial — **shipped on `main`, not yet tagged.** [PR #60](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/60) merged 2026-08-13, closing [#53](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/53) (§1 prefixed exports), [#54](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/54) (§8.4 fifth argument), [#55](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/55) (v0.7.1 flag spelling) and [#57](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/57) (§8.x bit constants un-exported). Verified at `main` post-merge: `src/lib_version.s` exports `LIB_CHACHA20_POLY1305_VERSION_*` / `_ABI_VERSION` with bare aliases under `.ifndef LIB_NO_BARE_EXPORTS`; no `.export` of any `LIB_SHARED_PRIMITIVES_*` bit constant; all five invocations pass `"CHACHA20_POLY1305"`. v0.5.0 `SHARED_CONSUMES` landed earlier via PR #56/#59. **Cell stays partial because the newest tag is still v0.6.0 (2026-07-28)** — `c64-wireguard` pins siblings by tag, so none of this is reachable downstream until a release is cut |

### v0.7.0 prefixed-exports migration (open)

Contract [v0.7.0](SPEC.md#12-changelog) resolved [#43](https://github.com/JC-000/c64-lib-contract/issues/43) — two libraries linked into one PRG collided on the unprefixed `LIB_VERSION_*` / `LIB_ABI_VERSION` (§1) and `LIB_PRECALC_<name>_*` (§8.4) symbols, so a consumer could import manifest equates from at most one of them. The fix is additive and needs no flag day, but it does need every adopter to act:

1. **§1** — export `LIB_<X>_VERSION_{MAJOR,MINOR,PATCH}` and `LIB_<X>_ABI_VERSION`, aliasing the bare names to them under `.ifndef LIB_NO_BARE_EXPORTS`, in a translation unit that exports nothing else.
2. **§5** — move the aggregate manifest equates out of `src/lib_version.s` into `src/lib_manifest.s`, so importing one cannot drag the bare §1 names into the link.
3. **§8.4** — refresh `src/precalc_table.inc` from this repo's canonical root copy and pass the library prefix as the fifth `LIB_PRECALC_TABLE` argument.

The bare names remain **required** through v0.x and are removed at v1.0, so nothing breaks while this wave lands. Until a given library ships it, a consumer composing that library with another still falls back to the §8.0 `od65` out-of-band pattern for it — noting per v0.7.2 that `od65` reads objects, not archives.

Status verified against each library's default branch and its newest tag on 2026-08-13. **Three of four have landed it on `main` — and none of the three is in a tag:**

| Library | Landed | Newest tag | Tag contains it? |
|---|---|---|---|
| `c64-nist-curves` | [PR #87](https://github.com/JC-000/c64-nist-curves/pull/87) | `v0.8.0` | **no** — verified, no `LIB_NISTCURVES_VERSION_*` at that ref |
| `c64-ChaCha20-Poly1305` | [PR #60](https://github.com/JC-000/c64-ChaCha20-Poly1305/pull/60) | `v0.6.0` | **no** |
| `c64-x25519` | [PR #82](https://github.com/JC-000/c64-x25519/pull/82) | `v0.8.0` | **no** — verified, no `LIB_X25519_VERSION_*` at that ref |
| `c64-polyval` | — | `v0.4.0` | n/a — [#21](https://github.com/JC-000/c64-polyval/issues/21)/[#22](https://github.com/JC-000/c64-polyval/issues/22) open |

**Tagged ≠ shipped, and right now nothing is tagged.** `c64-wireguard` pins sibling libraries by git tag rather than branch, so work sitting on an adopter's `main` is not reachable by the consumer this migration exists to unblock. All three landed rows are therefore ⚠️ partial, not ✅ — the collision is fixed in source and still live for the consumer. Three coordinated tags are what converts this wave into a result.

**A second collision class, found in the same audit, that v0.7.0 does NOT fix.** `c64-x25519` and `c64-ChaCha20-Poly1305` both `.export` the §8.x *bit constants* (`LIB_SHARED_PRIMITIVES_SQTAB`, `LIB_SHARED_PRIMITIVES_CT_MUL_8X8`) — unprefixed names with identical values in both. Reproduced on ca65 V2.18 + ld65 with both sides otherwise fully v0.7.0-migrated: `ld65: Error: Duplicate external identifier: 'LIB_SHARED_PRIMITIVES_CT_MUL_8X8'`; dropping the `.export` lines links clean. The SPEC presents these as local equates and never exports them, but never says so normatively — [contract #56](https://github.com/JC-000/c64-lib-contract/issues/56). Adopter-side: [c64-x25519#78](https://github.com/JC-000/c64-x25519/issues/78) item 3, [c64-ChaCha20-Poly1305#57](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/57) item 2. `c64-nist-curves` never exported them, which is why this only surfaces on the wireguard pair.

Priority note: `c64-x25519` + `c64-ChaCha20-Poly1305` are the pair actually linked together by `c64-wireguard`, and are the configuration that produced #43. Their migration is what converts that consumer's out-of-band archive check back into a link-time assert; the other two libraries gain collision-freedom but have no current two-library consumer waiting on them.

### Bootstrap-era issue summary

All adopter-side tracking issues filed 2026-05-20 as the bootstrap seed — every one is now closed (verified 2026-07-18; each maps to a ✅ shipped cell in the status table above):

- **c64-nist-curves**: [#40](https://github.com/JC-000/c64-nist-curves/issues/40) (minimal archives), [#41](https://github.com/JC-000/c64-nist-curves/issues/41) (segment rename), [#42](https://github.com/JC-000/c64-nist-curves/issues/42) (manifest equates)
- **c64-polyval**: [#12](https://github.com/JC-000/c64-polyval/issues/12) (`LIB_VERSION_*`), [#13](https://github.com/JC-000/c64-polyval/issues/13) (`.exportzp` header), [#14](https://github.com/JC-000/c64-polyval/issues/14) (segment rename), [#15](https://github.com/JC-000/c64-polyval/issues/15) (manifest equates), [#16](https://github.com/JC-000/c64-polyval/issues/16) (archive build targets)
- **c64-x25519**: [#43](https://github.com/JC-000/c64-x25519/issues/43) (REU bank `--asm-define`), [#44](https://github.com/JC-000/c64-x25519/issues/44) (`.exportzp` header), [#45](https://github.com/JC-000/c64-x25519/issues/45) (`LIB_VERSION_*`), [#46](https://github.com/JC-000/c64-x25519/issues/46) (manifest equates)
- **c64-ChaCha20-Poly1305**: [#26](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/26) (`.exportzp` header), [#28](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/28) (`LIB_VERSION_*`), [#29](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/29) (manifest equates)

Issues #43, #44, #26 were filed by [c64-wireguard](https://github.com/JC-000/c64-wireguard); the remaining nine were filed by [c64-https](https://github.com/JC-000/c64-https). All adopter-side maintainers see them as a single coherent ask.

## How to add your library

1. Read [SPEC.md](SPEC.md).
2. Open a PR against this repo adding a row to the status table above with current adoption state per section.
3. File tracking issues against your library's repo for each unimplemented section. Link them back to this contract repo in the issue body.
4. Cross-link this repo from your library's README so consumers can find the contract.
5. Update the row as sections land in subsequent releases.
6. **Enumerate precalculated tables** per SPEC [§8.0 catch loop](SPEC.md#80-bit-allocation-for-lib_x_shared_primitives). Required for every precalculated table that meets the §8.0 floor (≥ 256 B AND one of: REU-resident, hot-loop-read, page-aligned for fetch alignment). Two forms, both mandatory:
   - **`docs/precalc-tables.md`** in your library, one row per table: name, size, region (main RAM / REU bank / RODATA), source file, classification (curve-/algorithm-specific *or* potentially shareable), and the rationale for the classification.
   - **`LIB_PRECALC_TABLE` macro invocations** from one of your library's translation units. Copy `precalc_table.inc` verbatim from SPEC §8.0 into `src/precalc_table.inc`, `.include` it once, and emit one `LIB_PRECALC_TABLE "name", size, region, shared, "<X>"` per enumerated table, where `<X>` is your library prefix (the same one your §5 `LIB_<X>_*` equates use). As of contract v0.7.0 the macro exports six equates per table — the library-prefixed `LIB_<X>_PRECALC_<name>_{SIZE,REGION,SHARED}` plus the deprecated bare `LIB_PRECALC_<name>_*` triple, which is removed at v1.0. Cross-adopter audits grep on `_PRECALC_`, which matches both forms; the older `LIB_PRECALC_` pattern misses every prefixed export. The table *name* stays unprefixed — the prefix identifies the declaring library, never the table.

   Link `docs/precalc-tables.md` from your row in the status table above.

   **Intake reviewers MUST confirm step 6 is satisfied before merging the adopter PR.** A `LIB_PRECALC_*` export without a corresponding `docs/precalc-tables.md` row — or vice versa — blocks merge. A library that has no tables clearing the §8.0 floor still files `docs/precalc-tables.md` with a one-line "no tables above the floor" note and the rationale (e.g., "all internal scratch is single-byte ZP; rotation lookups are 16 B").
