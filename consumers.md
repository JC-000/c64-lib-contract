# Contract Consumers

This page tracks downstream projects that rely on the [c64-lib-contract](SPEC.md). Each row links to the consumer's lead architecture / ingestion doc.

## Consumer status

| Project | Status | Lead doc |
|---|---|---|
| [c64-https](https://github.com/JC-000/c64-https) | Integrated — contract alignment merged ([PR #55](https://github.com/JC-000/c64-https/pull/55)); pins now `libs/nistcurves` **v0.11.0** + `libs/x25519` **v0.11.1** (the "comb as a shipped product" release, merged 2026-08-15), past the remediated wave pair — the [#82](https://github.com/JC-000/c64-lib-contract/issues/82)/[#83](https://github.com/JC-000/c64-lib-contract/issues/83) `Duplicate external identifier` collisions are **no longer live here**; [c64-https#112](https://github.com/JC-000/c64-https/issues/112) is closed. Verified at the **v0.10.2 × v0.11.1** pair: all four backend x profile variants link, and the ECDSA KAT oracle passes 6/6 (3 valid accepted, 3 invalid rejected) on both the REU and FP_ONCHIP_MUL builds; the nistcurves v0.10.2 → v0.11.0 step is additive-MINOR with the default PRG byte-identical and ABI held at 2 (ref-verified at the tag), so the collision and link results transfer. ABI gates: nistcurves **2**, x25519 **3**. Asserted consumer-side at link time — §1 ABI generation, §3 REU bank budget, §5/§8.0 ownership tripwire (profile-dependent), §6.4 per-variant manifest discovery with an `od65` post-check, §6.6 footprint asserts on both backends, §13.3 rx-ring shape. Open consumer-side gaps: **§6.1** — the P-256 wrappers still edit archive member sets ([c64-https#119](https://github.com/JC-000/c64-https/issues/119)); the REU arms are retired, and the on-chip arms are blocked on the §8.3 deferral fix ([c64-nist-curves#123](https://github.com/JC-000/c64-nist-curves/issues/123), fixed in nist [PR #124](https://github.com/JC-000/c64-nist-curves/pull/124), awaiting a release to pin — the original blocker [nist#117](https://github.com/JC-000/c64-nist-curves/issues/117) shipped in v0.11.0's comb targets), which is why the comb profile is still excluded from the §6.6 assert and the §8.0 disjointness asserts are not yet writable; boot-cost mitigation for the comb table tracked as [c64-https#120](https://github.com/JC-000/c64-https/issues/120). **§13** net-ABI: origin surface; `src/net_abi.inc` is still documentation that nothing `.include`s, and the ip65 backend exports 6 of its 17 declared symbols — intake items per SPEC §13.8 ([c64-https#70](https://github.com/JC-000/c64-https/issues/70)) | [docs/library-ingestion-architecture.md](https://github.com/JC-000/c64-https/blob/master/docs/library-ingestion-architecture.md) |
| [c64-wireguard](https://github.com/JC-000/c64-wireguard) | Integrated & shipped — v1.0.0 (2026-07-28) links `libs/x25519` v0.8.0 + `libs/chacha20poly1305` v0.6.0 via §6 archives, §8.0 composition (x25519 owns sqtab/reu_mul/ct_mul), link-time §3/§8.0 asserts. Those interim gaps are all now closed — chacha [#47](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/47)/[#48](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/48) landed in chacha v0.7.0, contract [#41](https://github.com/JC-000/c64-lib-contract/issues/41)/[#43](https://github.com/JC-000/c64-lib-contract/issues/43) in v0.4.2/v0.7.0. **Master has since advanced past the v1.0.0 pins** to `libs/x25519` **v0.10.1** (`07f004b`) + `libs/chacha20poly1305` **v0.7.0** (`8d9f38b`), so it already carries the §1 prefixed exports and §4 segment renames. The current targets are the post-wave conformance releases — chacha **v0.9.0** and x25519 **v0.11.1** (both ref-verified; both carry the §2 ZP registry rename and the §6.6/§6.7 guards) — and taking either needs [c64-wireguard#52](https://github.com/JC-000/c64-wireguard/issues/52) first — this repo defines and exports the bare `zp_tmp*`/`zp_ptr*` itself rather than assembling a library `zp_config`, so it must export the canonical spellings and advance its `contract_asserts.s` chacha ABI pin (still `= 1`, stale since chacha generation 2). §13 net-ABI: forked surface; alignment + UCI-adapter resync per SPEC §13.8 ([c64-wireguard#48](https://github.com/JC-000/c64-wireguard/issues/48)) | [docs/library-ingestion-architecture.md](https://github.com/JC-000/c64-wireguard/blob/master/docs/library-ingestion-architecture.md) |

## How to add your project

1. Read [SPEC.md](SPEC.md).
2. Open a PR against this repo adding a row to the consumer table.
3. Link to your consumer's library-ingestion architecture / cfg-restructure doc.
4. If you need a new contract section (e.g., your consumer requires a property no current library exposes), open a PR against [SPEC.md](SPEC.md) with the proposed addition + rationale. Coordinate with current consumers via cross-linked PRs so the contract stays cross-consumer-coherent.
5. Cross-link this repo from your consumer's lead doc so library authors can navigate from your consumer back to the contract.
