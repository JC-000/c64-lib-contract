# Contract Consumers

This page tracks downstream projects that rely on the [c64-lib-contract](SPEC.md). Each row links to the consumer's lead architecture / ingestion doc.

## Consumer status

| Project | Status | Lead doc |
|---|---|---|
| [c64-https](https://github.com/JC-000/c64-https) | Integrated — contract alignment merged ([PR #55](https://github.com/JC-000/c64-https/pull/55)); pins now `libs/nistcurves` **v0.11.2** + `libs/x25519` **v0.11.2** (both exact tag commits, verified). **§6.1 is satisfied as of [c64-https PR #122](https://github.com/JC-000/c64-https/pull/122)** (merged 2026-08-16, three-round review; closes [c64-https#119](https://github.com/JC-000/c64-https/issues/119) with no successor): all archive member surgery is retired — no member drops, no generated glue TU, no `zp_config` rebuild/re-archive — the archives linked are **byte-for-byte upstream's `make` artifacts**. The §8.0 APP_OWNED shape is requested through §6.2 knobs: `CONTRACT_DEFINES` carries the four `SHARED_*` deferral switches + `LIB_NO_BARE_EXPORTS=1` + `LIB_SHARED_SQTAB_BASE=0xBC00`, and the three ZP overrides ride `CONTRACT_ZP_DEFINES`, proven by an `od65` post-check (canonical spellings, member named from `ar65 t` on the linked archive, plus a bare-`zp_ptr2` suppression guard — the §6.2 one-configuration defect found in review, fixed pre-merge). The previously-unwritable **§8.0 disjointness/coverage asserts are live**: the manifest attests full deferral — `SHARED_PRIMITIVES` **$0000** in every profile; `SHARED_CONSUMES` **$0007** (REU) / **$0005** (onchip, comb — the profile gate drops reu_mul from both masks; od65-measured, matches upstream's own `FP_ONCHIP_MUL` manifest asserts); the coverage assert carries an honest-scope note (unarmed while `APP_OWNED` covers all three allocated bits). Ownership moved app-side per §8.1/§8.3: `poly_prod_lo/hi` and `sqtab_lo/hi` are c64-https-owned in every profile (the onchip profile split is gone; `sqtab` address pinned at `$BC00` by a post-link check), and the §1 gate imports the prefixed `LIB_NISTCURVES_ABI_VERSION`. Evidence at the **v0.10.6 × v0.11.2** pair: ECDSA KAT oracle 6/6 on REU, onchip **and comb** (the comb run exercises the `poly_prod` rendezvous through `og_common`); REU PRGs byte-identical across the migration and all five PRGs byte-identical through both review-fix commits; `package-verify` 31/31 logic + 11/11 artifact, no skips. ABI gates: nistcurves **2**, x25519 **3**. Asserted consumer-side at link time — §1 ABI generation (prefixed), §3 REU bank budget, §5/§8.0 full-deferral attestation + disjointness/coverage, §6.4 per-variant manifest discovery, §6.6 footprint asserts on both backends including comb, §13.3 rx-ring shape. **§13 net ABI aligned** ([c64-https#142](https://github.com/JC-000/c64-https/pull/142), closes [c64-https#70](https://github.com/JC-000/c64-https/issues/70)): `src/net_abi.inc` is `.include`d by every network-touching TU and is the only import path for `net_*`; both backends export the full core/TCP/DNS surface (ip65 gained an error channel, `$41-$45`), ship `net_manifest.s` (ip65 with §13.7 blob equates), and the consumer ships `net_abi_asserts.s`. Remaining: comb boot-cost mitigation (table from disk) tracked as [c64-https#120](https://github.com/JC-000/c64-https/issues/120) | [docs/library-ingestion-architecture.md](https://github.com/JC-000/c64-https/blob/master/docs/library-ingestion-architecture.md) |
| [c64-wireguard](https://github.com/JC-000/c64-wireguard) | Integrated & shipped — v1.0.0 (2026-07-28) links `libs/x25519` v0.8.0 + `libs/chacha20poly1305` v0.6.0 via §6 archives, §8.0 composition (x25519 owns sqtab/reu_mul/ct_mul), link-time §3/§8.0 asserts. Those interim gaps are all now closed — chacha [#47](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/47)/[#48](https://github.com/JC-000/c64-ChaCha20-Poly1305/issues/48) landed in chacha v0.7.0, contract [#41](https://github.com/JC-000/c64-lib-contract/issues/41)/[#43](https://github.com/JC-000/c64-lib-contract/issues/43) in v0.4.2/v0.7.0. **Master has since advanced past the v1.0.0 pins** to `libs/x25519` **v0.10.1** (`07f004b`) + `libs/chacha20poly1305` **v0.7.0** (`8d9f38b`), so it already carries the §1 prefixed exports and §4 segment renames. The current targets are the post-wave conformance releases — chacha **v0.9.0** and x25519 **v0.11.1** (both ref-verified; both carry the §2 ZP registry rename and the §6.6/§6.7 guards) — and taking either needs [c64-wireguard#52](https://github.com/JC-000/c64-wireguard/issues/52) first — this repo defines and exports the bare `zp_tmp*`/`zp_ptr*` itself rather than assembling a library `zp_config`, so it must export the canonical spellings and advance its `contract_asserts.s` chacha ABI pin (still `= 1`, stale since chacha generation 2). §13 net-ABI: aligned in [c64-wireguard#67](https://github.com/JC-000/c64-wireguard/pull/67) + [#68](https://github.com/JC-000/c64-wireguard/pull/68) (`net_caps.inc` reference, §13.4 TOD start) + [#73](https://github.com/JC-000/c64-wireguard/pull/73) (`$8D`, close on teardown), all merged 2026-08-27 (`e4fc505`); remaining per SPEC §13.8: `net_manifest.s` + `net_abi_asserts` ([c64-wireguard#48](https://github.com/JC-000/c64-wireguard/issues/48)); §8.2 REU fix pending in c64-x25519 | [docs/library-ingestion-architecture.md](https://github.com/JC-000/c64-wireguard/blob/master/docs/library-ingestion-architecture.md) |

## How to add your project

1. Read [SPEC.md](SPEC.md).
2. Open a PR against this repo adding a row to the consumer table.
3. Link to your consumer's library-ingestion architecture / cfg-restructure doc.
4. If you need a new contract section (e.g., your consumer requires a property no current library exposes), open a PR against [SPEC.md](SPEC.md) with the proposed addition + rationale. Coordinate with current consumers via cross-linked PRs so the contract stays cross-consumer-coherent.
5. Cross-link this repo from your consumer's lead doc so library authors can navigate from your consumer back to the contract.
